#include<iostream>
using namespace std;
int main()
{
    int t;
    scanf("%d",&t);    
    while(t--)
    {
              int n;
             long long int a[10009],b[10009];
              scanf("%d",&n);
             
             long long int count=2,sum=0;
             for(int i=0;i<n;i++)
                     scanf("%lld",&a[i]);
                     
             b[0]=a[1]-a[0];
            sum=b[0];
             for(int i=2;i<n;i++)
             {
                    // b[i-1]=b[i-2]+count*(a[i]-a[i-1]);
                      b[i-1]=b[i-2]+count*(a[i]-a[i-1]);
                      sum+=b[i-1];
                     count++;
             }
             /*
             for(int i=0;i<n-1;i++)
             cout<<b[i]<<" ";
             cout<<endl;
             */
             printf("%lld\n",sum);
    }
    return 0;
}
